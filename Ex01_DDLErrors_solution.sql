-- If the company database doesn't exist, the USE statement
-- below will cause an error. The solution is to create the
-- database first and then point at it with the USE statement
-- You also need to divide the job in two. In MS SQL Server
-- the GO command can be used.

CREATE DATABASE Company;
GO

USE Company;
